README.md
🍽️ FoodProductsApi

API REST para gestionar productos y categorías, desarrollada en ASP.NET Core 8.0 con arquitectura por capas (Domain, Application, Infrastructure, API) y Entity Framework Core (SQLite).

🚀 Características principales

CRUD completo para FoodProduct y Category

Arquitectura limpia por capas (Domain, Application, Infrastructure, API)

Aplicación de principios SOLID

Validaciones dentro del dominio

Persistencia con Entity Framework Core (SQLite)

Documentación automática con Swagger

📋 Prerrequisitos

Antes de comenzar, asegúrate de tener instalado:

.NET 8 SDK

Herramienta CLI de migraciones: dotnet-ef

Git

🔧 Instalación y configuración
1️⃣ Clonar el repositorio
git clone https://github.com/tuUsuario/FoodProductsApi.git
cd FoodProductsApi

2️⃣ Restaurar paquetes NuGet
dotnet restore

3️⃣ Instalar la CLI de Entity Framework (si aún no está instalada)
dotnet tool install --global dotnet-ef

4️⃣ Aplicar migraciones y generar la base de datos

Asegúrate de ejecutar exactamente estos pasos para evitar conflictos entre versiones.

# 1. Navegar al directorio raíz del proyecto
cd c:\Users\Sergio\Desktop\j\FoodProducts

# 2. Limpiar compilaciones previas
dotnet clean

# 3. Reconstruir solución
dotnet build

# 4. Desinstalar cualquier versión incorrecta de EF CLI
dotnet tool uninstall -g dotnet-ef

# 5. Instalar la versión correcta (8.0.11 para .NET 8)
dotnet tool install -g dotnet-ef --version 8.0.11

# 6. Actualizar la base de datos (aplicar migraciones)
dotnet ef database update --project .\Products.Infrastructure --startup-project .\Products.API

5️⃣ Ejecutar la API
cd FoodProducts.API
dotnet run

6️⃣ Acceder a Swagger UI
https://localhost:5001/swagger

📂 Estructura del proyecto
FoodProductsApi/
├── FoodProducts.Domain/
├── FoodProducts.Application/
├── FoodProducts.Infrastructure/
└── FoodProducts.API/

Descripción de cada capa

Domain
Entidades, reglas de negocio, interfaces de repositorio y excepciones de dominio.

Application
Casos de uso y servicios que orquestan la lógica de negocio.

Infrastructure
DbContext, implementaciones de repositorios, migraciones y acceso a datos.

API
Controladores, configuración de dependencias, middleware y startup del proyecto.

🛠️ Endpoints disponibles

En Swagger UI podrás probar:

Categorías

GET /api/Categories

POST /api/Categories

PUT /api/Categories/{id}

DELETE /api/Categories/{id}

Productos

GET /api/FoodProducts

POST /api/FoodProducts

PUT /api/FoodProducts/{id}

DELETE /api/FoodProducts/{id}

Si quieres, también puedo:

✅ Mejorar diseño visual del README
✅ Agregar badges (build, .NET version, license, etc.)
✅ Agregar imágenes o diagramas de arquitectura
✅ Crear README tanto para backend como para frontend
